import SettingsForm from "@/components/app/settings/settings-form";

const Settings = () => {
  return (
    <div className="min-h-screen safe-paddings h-screen overflow-y-auto stylized-scroll p-10">
      <SettingsForm />
    </div>
  );
};

export default Settings;
